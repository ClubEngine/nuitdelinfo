<%= form_for @association, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :duck_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.collection_select :duck_id, Duck.all, :id, :name %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :gift_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.collection_select :gift_id, Gift.all, :id, :name %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :value, :class => 'control-label' %>
    <div class="controls">
      
      <%= f.text_field :value, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                associations_path, :class => 'btn' %>
  </div>
<% end %>
